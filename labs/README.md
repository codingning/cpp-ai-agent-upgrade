# 可提交训练实验

`labs/` 保存可以公开、可以复现的本人训练代码和测试。按 `weekNN/topic/` 或 `weekNN/name.cpp` 组织，并在对应 daily 记录中绑定构建命令与 commit。

`work/` 仅用于临时文件、公司相关实验、本地转储或其他不能上传的内容，默认被 Git 忽略。不要把 `work/` 中的内容当作作品集证据。

首次实现不提交 AI 给出的完整答案。可以先提交自己的失败版本，再通过测试和 Review 形成后续修正提交。
