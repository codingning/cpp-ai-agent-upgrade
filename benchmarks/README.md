# Benchmark 记录规范

性能数字必须同时记录：日期、机器/CPU/GPU/内存、操作系统、编译器、构建类型和选项、依赖 commit、模型及 hash、输入、线程数、warmup、迭代次数、正确性/误差阈值和原始输出。

至少报告适合该场景的 p50/p95/p99、吞吐、RSS/VRAM；LLM 场景区分冷/热启动、TTFT、prefill 与 decode tokens/s。只比较改变一个主要变量的实验，不用一次运行得出岗位或架构结论。

机器可读结果可以进入本目录；大型模型、trace、dump 和含隐私的原始日志不得提交，只保存脱敏摘要与本地路径说明。
