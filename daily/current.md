# 今日任务 · 2026-09-21（周一）· W3D1 · 1.5h + 🟣 追加档 25min

> 昨天你把 C++ 侧欠账清了个干净，知识图零 ❓，A4 也自核官方文档结了账。
> **W2 收官成绩是真的。但今天起翻页——W3 主题是 STL 容器，不是继续吃老本。**
>
> 本周唯一的硬指标：**MyVector<T> 三刀切完**。它已经从「有余力」升成 W3 门禁项，
> 因为它拖了两周。周一/二/四各 25min，**不占用当天原有时段**。

---

## 元规则（第 7 次要求，昨天首次做到，今天保持）

动手前先写一句：**「我认为 X 由 ___ 决定」**。
写不出来说明该查文档，不是该跑实验 —— 这种情况不罚，直接去查。
实验结果推翻预测时，**回头把结论行改掉**（昨天你做到了这一步，这是最值钱的动作）。

---

## 上午 10:30-11:10 · C++ 30min · std::vector

### 1. push_back 的「均摊 O(1)」

不要背结论，要能算。

- 先在纸上/注释里写：`push_back` 触发扩容时做了哪几件事？
- 如果每次扩容只加 1 个位置，push n 个元素总共搬了多少次数据？
- 如果每次扩容翻倍，push n 个元素总共搬了多少次数据？
- **「均摊」这个词到底在摊什么**——一句话说清

### 2. 迭代器失效（面试高频，今天只做 vector）

- `push_back` 之后，原来拿到的迭代器为什么**可能**失效？
- 「可能」这两个字从哪来？什么情况下不失效？

**当天验收**：上面 5 个问题，闭卷写在记录里，用自己的话。不查资料再写一遍算过。

> 📖 名词扫盲（非当日主角，各一行，防超纲）：
> - **迭代器（iterator）**：容器里「指向某个元素的位置」的对象，用法像指针（`*it` 取值，`++it` 往后）
> - **失效（invalidate）**：那个位置对象不再指向有效元素，继续用就是 UB
> - **均摊分析（amortized analysis）**：把偶尔一次很贵的操作，平摊到大量便宜操作上算平均代价

---

## 🟣 追加档 25min · MyVector 第 1 刀

文件：`F:\code\cpp-practice\week-03\my_vector.h`

**今天只要这个文件存在并且能编译，不要求功能完整。**

骨架（**这是骨架不是答案，函数体留给你**）：

```cpp
template <class T>
class MyVector {
public:
    MyVector();            // 分配 cap_ = 4 的空间
    ~MyVector();           // 释放
    void push_back(const T& v);   // 满了就 assert(false)，今天不管扩容
    size_t size() const;
private:
    T*     data_;
    size_t size_;
    size_t cap_;
};
```

- `assert` 是什么：`<cassert>` 里的宏，条件为假时立刻终止程序并打印哪一行断的。
  它是给**程序员错误**用的，不是给用户输入用的。
- 写完自己跑一次：push 4 个正常、push 第 5 个应该断在你写的那行。
  **跑之前先在注释里写死你猜的输出。**

> ⚠️ 别提前写扩容、别提前写 Rule of 5。第 2 刀（周二）才做扩容，第 3 刀（周四）才挂 Rule of 5。
> 这么切是为了让周四那刀变成「W2 学的整张表第一次落到自己的容器上」——索引挂钩，不是新知识。

---

## 下午 17:00-17:50 · Electron 45min · 引入 React

`F:\code\ai-desktop-assistant`

- 09-19 你已经用纯 HTML/CSS 搭了三段式 Chat UI v0（commit `3ec8791`）
- 今天把它**用 React 重写**，结构不变：消息区 + 输入区 + 左右气泡

**今天不做组件拆分**（那是周二的事），今天只要：React 跑起来、v0 的样子还在。

> 📖 先确认超纲范围：React / JSX / npm 构建链，**你哪些是完全没碰过的？**
> 记录里直接写出来，我明天补文档。按规矩，记录里没标你已掌握的，一律按没掌握处理——
> 不要硬扛，硬扛的结果是又一个「只是看过」。

---

## 记录 15min

`daily/history/2026-09-21.md`，五块：完成 / 掌握（闭卷自答）/ 未掌握卡壳 / 证据 / 明日计划。

**闭卷自答用自己的话，别抄我的解释。** 证据栏贴真实编译输出和运行输出。

---

## 本周待清（不在今天做，但别忘）

- **Utility vs Renderer 区别** —— 连续两次落地。出处我来实读
  `process_model_and_site_isolation.md` 确认后再布置，09-19 我在这翻过车，这次先查再给。
- **W1 周复盘三处空白** —— 跨周未补
- **块 2 九题中 4 条自标「只是看过」** —— 周日复盘前要转成闭卷可答
