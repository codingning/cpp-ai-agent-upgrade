# 今日任务 · 2026-09-22（周二）· W3D2 · 1.5h

> 昨天 200 分钟，MyVector 门禁一次清完，还自己加了三项计划外扩展、连改三版自己改对。
> **今天不是继续冲新内容的日子——今天是七天复测日。**
> 复测优先于一切新内容。不复测，L2 定级就只能按「候选」继续挂着。

---

## 元规则（第 8 次要求，09-20、09-21 连续两天做到，保持）

动手前先写一句：**「我认为 X 由 ___ 决定」**。
写不出来说明该查文档，不是该跑实验 —— 这种情况不罚，直接去查。
实验结果推翻预测时，**回头把结论行改掉**。

---

## 上午 10:30-11:10（40min）· 🔴 七天复测（C++ L2 定级）

**闭卷。不许翻 `daily/`、不许翻 `docs/`、不许翻 `F:\code\cpp-practice` 下任何 `.h`/`.cpp`。**
写不出来就写「推不出来」——**不罚，比抄上去有价值**。

### 复测一（15min）· 🔴 Q3 均摊推导重做

昨天这道你是**抄的**（写的 `4+8+12+...` 是等差，翻倍应为等比 `4+8+16+...`；
而且「S=2^k-4 → cap_<2n」那一步直接抄了我越线代写的 `.h` 第 29-38 行，我当日已认错）。

今天**只答一问**：

> **为什么最终的 `cap_` 一定小于 2n？**（n = 已 push_back 的元素个数）

只给你一个最小单位（不成链）：
> 容量是翻倍增长的。你最后一次扩容**之前**的容量是多少？扩容是在什么条件下触发的？

推不出来就写「推不出来，卡在 ___」，把卡点写具体。

### 复测二（10min）· 🟡 Q8 判据补全

昨天答「移动构造 noexcept → 移动，否则拷贝」，**漏了一种情况**。
把完整判据用自己的话重写一遍。提示只给一句：
> 如果这个类型**根本没有拷贝构造**（被 delete 了），`move_if_noexcept` 还有的选吗？

### 复测三（15min）· 🟡 Q9 异常安全 + 🟡 Q10 重答

- **Q9**（昨天你诚实写了「不太确定」，没扣分，今天补上）：
  copy-and-swap 为什么是异常安全的？
  提示（昨天给过，不再加码）：**看普通拷贝赋值从第一行到 return，`*this` 的三个成员分别在哪一行被修改。**
  然后问自己：如果在第二个成员改完、第三个没改时抛异常，对象现在是什么状态？

- **Q10**（昨天你标了「ai说，未验证」——**这一行是当日最值钱的动作**，区分「听来的」和「验证过的」）：
  但实验其实你已经做了：**证据 9 版本三**，路径 2 走 copy、路径 4 走 move，同一个函数两种分派。
  今天拿那组数据重答 Q10，并**把记录里「ai说」那三个字划掉**。

---

## 下午 17:00-17:50（50min）

### A. 🔴 Chromium/Electron 七天复测（15min）

只复测**已结账**的那条，不碰 Utility vs Renderer（出处我还没实读确认，不布置）。

> A4 三层链闭卷口述：**沙箱、contextIsolation、contextBridge 三层，每掉一层，攻击者分别多拿到什么？**
> （昨天你自核官方三篇后答过，今天看隔七天还在不在）

### B. React/JSX/Vite 概念课闭卷（20min）· 欠账

昨天讲完了题没出，今天补。**闭卷，不许翻昨天的聊天记录**：

1. React 解决的是什么问题？（不要答「它很流行」——答**不用它你会死在哪**，
   针对你的因果倒置高频坑）
2. JSX 算不算一门语言？为什么？
3. 为什么引入 React 就必须有构建工具？
4. Vite 是什么？它和「构建工具」是什么关系？
5. Electron 里引 React，和普通网页里引 React，有哪三点不同？

### C. 建 `docs/frontend-prereq.md`（15min）

这是计划缺陷的处置动作，我来起结构，**内容你填**——只有你知道自己实际会什么。
两栏对照：左栏「计划假设你会的」（我从 week-01~06 扫出来），右栏「你实际会的程度」
（四档：没听过 / 听过说不清 / 能看懂别人写的 / 能自己写）。

---

## 不做（写明，不是切掉）

- **Utility vs Renderer**：连续三次落地，但**出处未实读确认前不布置**——
  09-19 我在这里翻过车（给了不含答案的文档）。这条挂「主线必补」，
  由我先实读 `process_model_and_site_isolation.md` / `sandbox.md`，确认覆盖再出题。
- **q-framework D1（IOCP 异步 IPC）**：昨天排在 09-21 下午，实际没做（下午改了概念课）。
  今天是复测日，不塞新大活。**顺延到 09-23（周三）**，不作废。
- **week-03 原排周二内容**（map vs unordered_map、Chat UI 组件拆分）：
  组件拆分依赖前端前置断层先补完，**顺延**；map/unordered_map 挪到周三与迭代器失效合并。
  两条都**不作废**，已记账。
- 昨天自评超纲的 2 项（元素级计数 vs 类级打印为何后者无分辨力 / `push_back` 用 swap 改造）：
  **扩展项**，今天复测占满就不上；有余力再做，做了必须写进当日记录。

---

## 🔴 待你确认（昨天没回，今天第一句话回我）

git log 实查与你 09-21 自述有三处出入（详见 `docs/q-framework-map.md` 第二节），
**影响简历怎么写**：

1. `shell_extension/` **6/6 全是你**，你只说「主要负责」——**你低估了自己**
2. `service/` 你有 9 次第一作者提交，你**完全没提**
3. `components/compress/` 只有 2 次提交，但你说「主要负责照片压缩」——
   **照片压缩的代码是不是在别的路径？给我具体位置，我重新核**

---

## 收工前（15min 记录）

`daily/history/2026-09-22.md`，五栏照旧：完成 / 掌握（闭卷自答）/ 未掌握卡壳 / 证据 / 明日计划。

**复测日的记录多一栏**：把今天四道复测题的结果如实标成三档——
**「独立答对」/「提示后答对」/「推不出来」**。
七天复测的意义就是量这个，标漂亮了等于白测。
