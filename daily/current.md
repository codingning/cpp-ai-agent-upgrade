# 今日任务（2026-09-04）

## 最低单元（约 60 分钟）

创建 `work/raii_lab.cpp`，实现并测试两个函数：

1. `std::string safe_name()`：按值返回字符串；
2. `int first_duplicate(const std::vector<int>&)`：先写你能独立完成的版本，再记录 AI 辅助后的改动。

至少写 5 个测试：空数组、单元素、无重复、重复、负数。先闭卷 20 分钟；然后可使用 AI。不要追求复杂框架，能用当前编译器运行即可。

## 验收

- 编译成功；
- 5 个测试全部通过；
- 写下两个函数的时间/空间复杂度；
- 记录一个自己不会或被 AI 修正的点；
- 未完成时只记录阻塞，不补写结论。

## 完成后反馈

运行 `scripts/record-progress.ps1`，填写实际分钟、输出和阻塞；下一天任务依据记录生成。
