# 今日任务：2026-09-16（周三）· W2D3

> 昨日（09-15）完整完成 + 超额，3h。今天正常推进，但**上下午各带一条 W1 补课**。

---

## 上午 10:30-11:10 · C++（40min）

### 主线：Rule of Zero

什么时候一个特殊成员函数都不用写？

闭卷先答，不许先查：

1. 什么样的类可以一个特殊成员函数都不写，还完全安全？
2. 你昨天写的 MyString 能不能改成 Rule of Zero？改完还叫"手写 String"吗？
3. Rule of 0 / 3 / 5，这三个数字之间是什么关系？

产出：举 3 个例子，说明只用 STL 容器做成员就不需要写任何特殊成员函数。

### 🔴 补课 A3：move-from 对象的状态（W1 周五计划项，一直没做）

`std::move(a)` 之后，`a` 是什么？

- 还能不能用？
- 调用它的成员函数会不会崩？
- 它的值是什么？

**必须用实跑输出证明，不许只写结论。** 关键词「有效但未指定」（valid but unspecified）。

建议做法：拿你昨天的 MyString（有堆指针的那个）和一个 `std::string` 各测一遍，对比两者行为。

**验收**：能说清"为什么标准不规定 move 之后的具体值"，并给出一条实际编码建议。

---

## 下午 17:00-17:50 · Electron / Chromium（50min）

### 🔴 补课 A4（本日优先级最高）：preload 与沙箱

W1D2 计划里的那道题，一直欠着：**为什么删掉 `preload.js` 后网页里访问 `process` 会失败？**

分三步做，别跳：

**第一步（动手）**：真的去 `F:\code\ai-desktop-assistant` 把 preload 摘掉，跑一次，把报错原文抄下来。不要凭想象答。

**第二步（读）**：昨天你自己找到的两篇，今天读第一篇：
- https://chromium.googlesource.com/chromium/src/+/HEAD/docs/design/sandbox.md

只读到「Sandbox 的目标和基本模型」为止，FAQ 那篇今天不读。

**第三步（连起来）**：回答这三问 ——
1. `contextIsolation` 是什么？它隔离的是谁和谁？
2. 渲染进程被限制访问文件系统（昨天读到的），和 preload 必须存在，是不是同一件事的两面？
3. 如果 `nodeIntegration: true` 直接开着会怎样？为什么 Electron 官方把它默认关掉了？

**验收**：能把「Chromium 沙箱 → Electron contextIsolation → preload 作为唯一通道」这条链讲通。第 3 问要能说出具体的攻击后果，不要只说"不安全"。

### 原计划降级

BrowserWindow API 多窗口改代码 → **顺延到周六**，今天只读文档不动手。理由：A4 这条链比多开一个窗口重要得多，它是你简历上「Electron + 浏览器安全」这个卖点的地基。

---

## 记录（15min）→ `daily/history/2026-09-16.md`

固定动作，别漏：
- 「今日一句话复述」栏（昨天新加的，坚持住）
- 犯的错照实写，不许删

---

## 今日验收清单

- [ ] Rule of Zero 三问 + 3 个例子
- [ ] move-from 状态实验，有实跑输出
- [ ] preload 摘掉后的真实报错原文
- [ ] sandbox.md 读完（只读到基本模型）
- [ ] 三问连成一条链
- [ ] 记录写完 + commit

---

## 进阶（有余力再做，没时间直接跳过）

昨天实验矩阵里，`std::move` 退化成拷贝**没有任何警告**。
查一下：MSVC 或 clang 有没有编译选项能让这种退化被检测出来？
如果没有，工程上怎么防？（提示：想想你们部门 code review 会怎么做）
