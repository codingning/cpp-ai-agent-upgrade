# 🚫 停训 · 2026-09-25（周五）~ 2026-09-27（周日）· 中秋

> 依据 `docs/calendar-holidays.md`（国办发明电〔2025〕7 号，教练 09-23 实查，多源一致）。
> 本人 09-23 立的规矩：**假期不留作业、不设「有空看看」、不做任何变相排课。**
> 三条 cron 已加停训守卫，09-25~09-27 自动静默，不需手工开关。

**这三天没有任务。这一页不是委婉的作业页，就是空的。**

下一个训练日：**2026-09-28（周一）· W4D1**。

---

## 09-28（周一）W4D1 · 预排（教练当天早上会重算容量后定稿）

> ⚠️ 排课口径变了：09-24 下午段零产出，欠的东西跨了 4 天。
> **09-28 不是「接着往下」，是「先还账再往下」。**

### 上午 10:30-11:10（40min）· C++ 清欠（5 条，都是小题）

09-24 上午两轮答过一遍，过 3 欠 3，另有一条两次绕开的。

1. **key 要求订正**：`map` 要的是 `operator<`（不是你答的 `operator>`）；
   `unordered_map` 要 **hash + ___**，把你漏的那个名字补上。
   延伸：为什么有了 hash 还非要它？（想两个不同 key 撞进同一个桶）
2. **`unordered_map::insert` 后迭代器失效吗** —— 你答「记不得了」，我只给了出处没给答案。
   cppreference `unordered_map::insert` 页，「If after the operation...」那段
   （关键词 `rehashing` / `invalidated`）。**读完写一句结论。**
3. **有序遍历 vs 动态范围查询，差的那一层**：你答「插入打乱排序」不严谨 ——
   插到 `lower_bound` 算出的正确位置上，`vector` 照样有序。
   真代价在「后面的元素要 ___」，这个是 O(?)，`map` 是 O(?)。填完闭环。
4. 🔴 **第三次问：q-framework 里哪份数据非 `map` 不可？**
   09-23 问过、09-24 两轮都绕开了。**允许答「我翻了，没找到」，不允许继续不答。**
5. **重跑一次 rehash 探针**（`week-03/key_probe.cpp`）——
   你 09-24 自己说了「那个实验是你做的不是我做的」，说得对。按你自己的规矩，
   二手结论不算掌握。跑一遍，看 `bucket_count` 和元素地址。

> **它是什么 · `operator==`**：C++ 里判断两个对象「相等」的运算符重载。这里先不解释它为什么
> 和 hash 配对出现，那一问留给你自己想。

### 下午 17:00-17:50（50min）· 前端断层第一课（按零基础重来排，不是复习）

隔了 4 天，这两段一次都没动过手，**当第一次讲**。

**A. npm 工具链亲手跑（15min）**
`F:\code\` 下新建空目录 → `npm init -y` → 逐行看 `package.json` →
手加 `"hello": "node -e \"console.log(1+1)\""` → `npm run hello`。
**闭卷答**：`npm run hello` 时 npm 做了什么？它**怎么知道** `hello` 是什么？

**B. `MessageItem` 组件 + props（30min）**
在 `F:\code\vite_react\vite-project` 里动手（已经能跑，别新建工程）。
写一个 `MessageItem`，收一个 `text` 显示出来；在 `App.tsx` 里用**三次**，传三个不同字符串。
两版都写（这是骨架，函数体你自己填）：

```
function MessageItem(props) { ... }         // 第一版：用 props.text
const MessageItem = ({ text }) => ( ... )   // 第二版：箭头函数 + 解构
```

第二版是你自评「不会」的那两条 JS 语法的**第一次实战**。

**动手前写一句**：我认为 props 改成 `({ text })` 之后，函数体里要写 ___ 而不是 ___。

### 收工前（顺延自 09-24，未作废）

1. **Chromium 三层链整链口述**（sandbox → contextIsolation → contextBridge，
   每层「掉了会怎样」+「为什么开关独立但防护串联」）。过了 L1+ → **L2**。
2. **Utility vs Renderer 闭卷两问** —— 出处我 09-24 已实抓给你（见 `daily/history/2026-09-24.md`
   或 commit `3e6893d`），两段原文可核。我只给出处不给答案。
3. **三仓推完**：`vite-project` 先 `git init`（`.gitignore` 挡 `node_modules/` `dist/`），
   09-23 写的 `main.cjs`、`electron-smoke.cjs` 至今零版本记录，已挂 5 天。

### 关于 q-framework 对照轨

原定 09-28 起跑（`docs/q-framework-track.md`）。但当天要先还 5 条 C++ 账 + 前端零基础重来，
**教练 09-28 早上推送前重算容量再定是否当天挂载**，不硬塞三线并行。

---

## 📌 09-24 的账，一条没抹

- 上午 5 问：**过 3 条**（rehash 机制 / `vector::insert` 失效范围 / `lower_bound` 的 end 触发条件）
- 下午段**零产出**，收工三件全未做，**原因你没回话，我没替你编** ——
  记录里那栏就空着写「原因本人未回话」
- 「假期后第一件事」清单**没写**
- 我自己也错了一次：21:39 那条说「今天两段窗口产出是 0」是错的。上午你答了两轮，
  我只查了文件时间戳和 git log，**没查会话记录**。同类第 3 次，已写进记录第六节
