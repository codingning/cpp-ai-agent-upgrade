# 今日任务 · 2026-09-17（周四）· W2D4

> 昨天（09-16）是**部分完成日**：上午 C++ 做了、闭卷没过关；下午 A4 只走完 1/3；
> 三个仓库零 commit、记录全由教练 23:00 补。
> **今天是收尾日，不是新内容日。** 时间不够就砍下面的「可选」，不许砍「必做」。

---

## 上午 10:30-11:10（C++）

### 必做 · 开场 15 分钟：清昨天的尾（闭卷，不许翻 2026-09-16.md）

四题，写在 `daily/history/2026-09-17.md` 里，用自己的话：

1. **Rule of 0 / 3 / 5 分别是什么？** 各一句话，说清「触发条件」和「要求你做什么」。
   （昨天你说"我现在不知道 Rule of 0/3/5 分别是什么"——但 09-15 你自己写过
   "拷贝类和析构函数会抑制移动构造/移动赋值"。机制你有，缺的是名字。今天把名字挂上去。）

2. **哪三个是 C++98 就有的？哪两个是 C++11 才加的？**
   （昨天这题答错了：你写「C++98 是析构、拷贝构造、**移动构造**；C++11 加了**拷贝赋值**和移动赋值」。
   两个位置对调了。**今天重答，不许抄昨天的记录文件。**答完再去翻 day2_gen_rules.cpp 自己对。）

3. **第一问重新收紧措辞**：什么样的类可以一个特殊成员函数都不写还完全安全？
   （昨天答"不需要管理资源申请和释放的类"——任何类的成员都要构造和销毁，这句字面上说不通。
   你在追问里答对了判据：`int* p` 是裸资源。把那个判据写进正式答案。）

4. **Test3 里，是哪个成员导致 `rule3` 的拷贝构造被隐式删除的？为什么？**
   （你昨天把 `error C2280: 尝试引用已删除的函数` 抄进代码注释了。
   **抄下报错 ≠ 理解机制。**今天要能说出是哪个成员、它为什么会传染给外层类。）

### 必做 · A3 两条结论（昨天有数据无结论）

Test4 的六行输出昨天已经跑出来了（MyString size 0 / std::string 打印空行 + size 0）。
今天只写两句话，不用再跑代码：

- 为什么 C++ 标准**不规定** move 之后源对象的具体值？（提示：标准要给实现留什么自由）
- 由此给自己一条**编码建议**：move 完的对象，什么操作能做、什么操作不能做？

### 剩余时间 · A1 异常路径 RAII（W1 欠账，本周门禁项）

写一个小实验：函数里 `new` 一块内存，中间 `throw`，对比
「裸指针版」和「RAII 版（你自己的 MyUniquePtr 或 unique_ptr）」两条路径下析构有没有被调用。

- 不给你代码。你已经写过 MyUniquePtr，直接复用。
- 验收：能看见裸指针版**漏掉了**析构输出、RAII 版**打印了**析构输出。
- 文件名 `week-02/day4_exception_raii.cpp`。

### 可选（时间够才做）· B1：给 MyUniquePtr 补 `assert` 单元测试

`assert` 是什么：C 标准库的运行期断言宏，条件为假就打印表达式并终止进程。
要 `#include <cassert>`。三个点起步：move 后源指针为空、`operator bool` 行为、析构只走一次。

---

## 下午 17:00-17:50（Electron / Chromium）

**今天下午只有一件事：把 A4 剩下的 2/3 清掉。** 不开 Menu API，Menu 顺延周六。

### 第二步 · 读 Chromium `sandbox.md`

只读「Sandbox 的目标」和「基本模型」两节，**不要往下读实现细节**。
读完合上，用中文写三句话：沙箱在防谁、沙箱怎么防、渲染进程被攻破后攻击者卡在哪一层。

### 第三步 · 三问（闭卷，写进当天记录）

1. `contextIsolation` **隔离的是谁和谁**？（不是"隔离渲染进程和主进程"，再想想）
2. 沙箱（sandbox）和 preload 是不是一体两面？说清各自管什么。
3. `nodeIntegration: true` 的**具体攻击后果**是什么？
   你昨天已经亲手看见"摘掉 preload 后渲染进程报错"这条证据了——
   把它接进来：一个 XSS 打进渲染进程，在 `nodeIntegration: true` 下他能干什么？

---

## 收尾（必做，昨天整条没做）

昨天三个仓库零 commit。今天不许再漏：

- [ ] `F:\code\cpp-practice`：`git add -A && git commit`（**这个仓没有 remote，不要 push**）
- [ ] 昨天的 `day3_rule_of_zero.cpp` 也在里面，一起提交
- [ ] `F:\code\ai-desktop-assistant`：如果今天有真实改动才提交，没有就跳过
- [ ] 自己写 `daily/history/2026-09-17.md`（**自己写，不要等教练 23:00 补**）
- [ ] 写「今日一句话复述」（09-15 起的固定动作，昨天漏了）

---

## 今天的元规则（第三次了）

09-15 traits 输出没变、09-15 typeid 一样、09-16 `npm start` 终端没报错——
三次都是**同一个动作没做**：没问「我看的这个指标，真的能反映我想测的东西吗」。

今天每做一个实验，运行之前先写一行：**我预测输出是 ___，如果不是，说明我哪里想错了。**
写在代码注释里。这是今天最重要的一条，比任何知识点都重要。
