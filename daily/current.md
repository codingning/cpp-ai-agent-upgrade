# 今日任务 · 2026-09-18（周五）· W2D5 · 🔵 本周收束日

> 昨天（09-17）约 200min，是 W2 最扎实的一天：欠账全清 + 门禁两项达成 + 原定内容也动了手。
> **今天不加新知识。** 今天只干一件事：把 W1+W2 学过的散点**收成一张表、一张图**。
> 这是 09-22 七天复测的预演——现在错没关系，下周还错就说明学法有问题。

---

## 开场 10 分钟 · 清 09-17 的尾（先做完再开始正题）

- [ ] `F:\code\cpp-practice`：`git add -A && git commit`（`day4_noncopyable.cpp` 昨天没提交。**这个仓没有 remote，不要 push**）
- [ ] `F:\code\ai-desktop-assistant`：`menu.js` 还是 untracked，接线后一起提交
- [ ] 回 `daily/history/2026-09-17.md` 补写「今日一句话复述」（连续两天空着了）

**三处空白，各一句话补进 09-17 的记录里**：

1. `T b = std::move(a);` 为什么容易翻车？
   —— 你代码里恰好躲过了：因为你**显式写了** `NonCopyable(NonCopyable&&)`。
   把那行删掉再编一次，看报什么。先写预测再编译。
2. 析构 `protected` 的「为什么」——你已经实测出 C2248 了，但没说这个设计**防的是什么**。
   提示：`NonCopyable* p = new Widget(1); delete p;` 这句话里藏着答案。
3. `= default` 和「什么都不写」到底差在哪？
   —— 昨天你答的是「生成规则」，那是另一题。区别落在**「用户声明」这个身份**上：
   `= default` 算不算用户声明？它算了以后会连带影响什么？自己查，别问我。

---

## 上午 10:30-11:10（C++ 40min）· 🔴 闭卷默写整张表

**不看任何资料、不看自己的代码、不看历史记录。** 纸上或新建文件，限时 25 分钟：

1. **6 个特殊成员函数**，全名 + 函数签名写出来
   （提示：不是 5 个。默认构造也算，只是不进 Rule of 3/5）
2. **Rule of 0 / 3 / 5** 各是什么，三个数字之间是什么关系
3. **一张抑制关系表**：写了 A 会不会影响 B 的自动生成？只填「影响 / 不影响」
   最少覆盖这几格：写析构 → 移动构造？写拷贝构造 → 移动赋值？写移动构造 → 拷贝构造？
4. **哪几种情况 `std::move` 会静默退化成拷贝**？各举一个最小例子

写完再对 `week-02/day2_gen_rules.cpp` 的实跑输出核对，**标出错了几处**，错几处就是几处，别改答案。

---

## 下午 17:00-17:50（收束 50min）· 画图，不写代码

### 1. W1+W2 C++ 概念关系图（30min）

把两周学过的 C++ 概念串成一张图，存 `docs/cpp-concept-map-w2.jpg`（手绘拍照即可）。

起点路线建议：
`RAII → 资源所有权 → 为什么需要拷贝/移动 → Rule of 5 → 抑制规则 → 退化陷阱`

**硬要求：图上每个节点必须能追溯到你自己写过的一个文件或一次实验。**
写不上出处的节点，说明那个概念你只是「听说过」——直接标个问号留在图上，别删。
问号就是下周的复习清单。

参考你自己的产出：`day1_string.cpp`（MyString / Rule of 5）、`day2_gen_rules.cpp`（10 用例矩阵）、
`day3_rule_of_zero.cpp`（Test1 vs Test2 退化对照）、`day4_exception_raii.cpp`（栈展开）、
`day4_uniqueptr_test.cpp`（assert 测试）、`day4_noncopyable.cpp`（mixin + `= delete` 禁重载）。

### 2. 核对 `daily/progress.jsonl`（10min）

本周五天数据是否齐、时长是否真实。不真实就改，别护着数字。

### 3. 本周门禁自测（10min）

对 `plan/weekly/week-02.md` 末尾「本周门禁」逐条判定，**过 / 不过，不许写「差不多」**。
已知：A1 异常路径 RAII ✅、B1 MyUniquePtr 单元测试 ✅。其余自己判。

---

## 有余力才做（不占上面的时间）

- **Menu 接线**：`main.js` 里 require 你写的 `menu.js` + `Menu.setApplicationMenu(...)`，
  `npm start` 跑起来**亲眼看见菜单**，然后回答昨天那题：菜单是全局一份还是每窗口一份？
  你猜的是「每个窗口一份」——跑完回头对。
- **Q11/Q12 矛盾归零**：你说「沙箱和 preload 各管各的」，又说「`nodeIntegration: true` 会关掉沙箱」。
  这两句不能同时成立。去 Electron 官方 webPreferences 文档查 `sandbox` 和 `nodeIntegration`
  是不是两个独立开关、开了一个另一个会怎样。**查完用一句话把矛盾消掉。**

---

## 今天必须确认的一件事（和学习无关，但影响周六）

**实测 ToDesk 能不能远程连上办公机。** 09-19 是死线，今天就试。

- 连得上 → 周六按 90min 保守排（Chat UI v0 + 进程模型图 + 博客大纲）
- 连不上 → 周六整日跳过，不欠账不追赶，今晚就告诉我，我重排

---

## 今天的元规则（第四次了，也是最后一次提醒）

09-15 traits 输出、09-15 typeid、09-16 `npm start` 无报错、09-17 noncopyable 全程无预测——
**同一个动作四次没做**：运行之前先写「我预测输出是 ___，如果不是，说明我哪里想错了」。

但昨天 Menu 那题你**写了猜测**。所以你不是不会做，是只在被问到的时候才做。
今天上午的闭卷默写，本质就是一次大型预测——写完再去对 `day2_gen_rules.cpp`。
**这个动作从今天起变成习惯，不再靠我提醒。**
