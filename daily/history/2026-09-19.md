# 2026-09-19（周六）· W2D6 · ToDesk 远程日

> 本记录由教练 23:00 依实查证据补写（当日未自建记录文件）。
> 闭卷自答栏**留空不代写**，明天周复盘时本人补。
> 本日时长按文件 mtime + commit 时间推算：**约 140min（13:50 起～16:09）**，
> 其中 13:50 之前无任何文件改动证据，不计入。

---

## 一、完成（实查证据支撑）

### 1. 主任务 · Chromium 四进程模型图 ✅

`docs/chromium-process-model.drawio`（8568 字节，mtime 16:00，commit `bf8ab4e`）

图上已有的内容（教练读 XML 实查）：
- Browser Process：标注「唯一不在沙箱里的，管窗口/网络/文件/磁盘，是所有资源的守门人」，并标了「对应 main process」
- Renderer Process：「跑 Blink 渲染网页和 JS，处理来自互联网的不可信数据」，标「沙箱最严」
- GPU Process：「真正的显卡驱动调用发生在这里」，标「沙箱权限较宽——必须访问显卡」
- Utility Process：「跑短命工具服务，网络服务默认就在这儿」，标「沙箱」
- 通信边标了 MOJO；Browser 侧另标了「磁盘 / 文件句柄」

**这四句职责描述全部与官方资料对得上**，尤其 Utility 里「网络服务默认在这儿」是从
`services/network/README.md` 读出来的，不是猜的。

### 2. 补 B2 · Chat UI v0 ✅（欠 5 天，本周门禁项）

`F:\code\ai-desktop-assistant\index.html`（4796 字节，mtime 15:57，commit `3ec8791`）

实查结构：三段式 flex 纵向布局（`display:flex` + `flex-direction:column`），
顶部保留原 IPC 控件区（`flex:0 0 auto`），中间消息区 `flex:1 1 auto` + `overflow-y:auto`，
底部输入区 `flex:0 0 auto`；消息用 `.msg-row me/other` + `.bubble` 做左右分栏气泡
（`justify-content:flex-end/flex-start`，`border-radius:10px`），预置 4 条示例消息。

达标：纯 HTML/CSS、无 React、无模型接入，符合当日「只要静态能看」的要求。

### 3. 补 B4 · 博客大纲 ✅建立 / ⚠️质量不达标（本周门禁项）

`blog-drafts/01-why-electron-ai-assistant.md`（625 字节，mtime 16:06）
三节标题齐（什么是 Electron / 为什么是 AI / 为什么现在），但**每节只有一段话，不是可写作的大纲**。
教练 16:07 已指出四处待改，本人当日未改，已挂进 `daily/current.md` 的收尾遗留区。

---

## 二、掌握（闭卷自答 · 本人填，教练不代写）

> 明天周复盘时闭卷答，答完标「自己想的 / 抄的」。

1. GPU 进程为什么要单开一个？（当日动手前应写的预测栏空缺，现在补答）
   - 答：
2. Network Service 默认跑在哪个进程？你是从哪句话读出来的？
   - 答：
3. Utility 和 Renderer 都是 Browser 创建的沙箱进程，区别是什么？
   - 答：
4. 「Sandbox operates at process-level granularity」这句话，推翻了你之前哪个说法？
   - 答：
5. 一个 `BrowserWindow` 在 Electron 主进程这边是什么对象？它对应 Chromium 的谁？
   - 答：

---

## 三、未掌握 / 卡壳 / 遗留（教练实查）

1. **进程模型图 cell 48 填的是错答**：现内容为「RenderProcessHost 对应 electron 的 ipcmain」，
   而**同一张图的黄色注释框里已经写明** ipcMain/ipcRenderer = Browser 与 Renderer 之间的
   Mojo 消息管道，「别和左右两个 RenderProcess\* 框混为一谈：那两个是进程的代表对象，
   这个是通话的那条线」。**图上自相矛盾**。这已是这个对应关系第三次出错（09-15 答反一次）。
2. **cell 49 仍空**：RenderProcess 对应 Electron 什么，未填。
3. **博客大纲四处未改**（详见 current.md）：第一节标题与内容不符、三节都缺子条目、
   「在 future 中保持竞争力」夹英文、第三节只有「因为焦虑」没往下追一层。
4. **元规则「运行前先写预测」第六次未见执行**：current.md 明确要求写「我猜 GPU 进程独立出来
   是因为 ___ / 我猜 Utility 进程装的是 ___」，仓库内未找到任何书面预测。
   如果写在别处，明天贴出来，教练改判。
5. **当日无自建学习记录文件**（本文件为教练补写）——这是 09-16 之后第二次。
6. 09-18 的旧欠账仍未动：`= default` vs 什么都不写、析构 `protected` 的为什么、
   Q11/Q12 沙箱矛盾未自行核实官方文档、noexcept→vector 扩容无本人实验。

---

## 四、教练自身错误记录（当日 4 次，供明天核对计划可靠性）

1. 14:10 给的 `mojo_and_services.md` 读法照旧版写，该文档已被上游改写
2. 14:30 该文档根本不含 GPU 与 Utility/Renderer 对比，出题超出资料范围（**本人读完指出**）
3. 15:17 曾断言「Renderer 不直接跟 GPU 说话」——错，官方 `debugging_gpu_related_code.md`
   原文说 GL 调用直接发往 GPU 进程
4. 15:40 要求删 `overflow-y:auto` 做对照实验，但 week-01~06 从未训练过 CSS 布局
   （**本人指出**，B2 据此降级）

**另**：13:50 本人自查发现教练漏排 week-02.md 周六原定的 B2、B4 两个门禁项（教练第二次犯
「把新排内容当成替代原计划」的错），已按门禁补回。这次自查质量高于教练初排。

---

## 五、证据清单（教练实查，非推测）

| 项 | 证据 |
|---|---|
| 进程模型图 | `docs/chromium-process-model.drawio` 8568B，mtime 09-19 16:00，commit `bf8ab4e`（+78 行） |
| 博客大纲 | `blog-drafts/01-why-electron-ai-assistant.md` 625B，mtime 16:06，同 commit（+7 行） |
| Chat UI | `F:\code\ai-desktop-assistant\index.html` 4796B，mtime 15:57，commit `3ec8791`；CSS 实查到三段 flex + overflow-y + 气泡圆角 |
| 时长 | 首个改动 13:50（current.md 补排时间戳）→ 末次 commit 16:09，约 140min |
| 当日未写记录 | `daily/history/2026-09-19.md` 在 23:00 前不存在 |
| C++ 侧零产出 | `F:\code\cpp-practice` 最新 commit 仍是 09-18 的 `013c4c2`，week-02 目录最新 mtime 09-18 10:26 |

**本日无 C++ 代码产出**——周六原是 C++ 综合日，今天全给了 Chromium/Electron 与门禁补课。
不算失误（门禁优先），但 W3 是 STL 容器周，`Vector<T>` 这条线连续两周没动，要在明天的
「下周调整」里明确处理。

---

## 六、明日计划（09-20 周日 · W2 周复盘 · 约 1h）

见 `daily/current.md`。
