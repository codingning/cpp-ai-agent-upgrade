# 每日训练记录：2026-09-22（W3D2 · 七天复测日）

> ⚠️ 本文件为**上午段**记录，下午 17:00 段（Electron 复测 + React 闭卷 + frontend-prereq.md）
> 完成后追加。`progress.jsonl` 按惯例 23:00 一次性写入，不在此处预写。

## 今日状态

- 状态：上午段已学习
- 实际投入：上午 10:33-11:0x（约 35min，含教练往返批改）
- 主题与 A/B/C 日：C++ L2 七天定级复测（闭卷）
- 开始等级 / 结束候选等级：C++ L2 候选 / **L2 达成**

## 闭卷诊断（四题，本人自答原文见下）

复测口径：每题标「独立答对 / 提示后答对 / 推不出来」。

### Q3 均摊推导：为什么最终 cap_ 一定小于 2n —— **独立答对**

> 因为每次扩容前的容量都是 cap，只有插入的个数超过 cap 时才会扩容，扩容后是 2*cap。
> 添加到 n 个时假设发生了 k 次扩容，总搬运 S = 4+8+16+...+2^(k-1) = 2^k-4；
> 2^k 就是现在的 cap。只有当 cap/2 装满以后才会触发最后一次扩容，
> 也就是 cap/2 + 1 <= n，即 cap <= 2n-2，也就是 cap < 2n。

- 推翻 09-21「抄答案」的判定：这次是本人自推，且抓住了**触发条件**这个关键环节
  （09-21 抄的那版正是卡在「S 与 n 之间凭什么有不等式」）。
- **教练实跑补边界**（n=1..100 探针，MSVC /std:c++17）：

  ```
  n=1  cap=4  2n-2=0   [不成立]
  n=2  cap=4  2n-2=2   [不成立]
  n=3  cap=4  2n-2=4   [成立]
  n=5  cap=8  2n-2=8   [成立，取等 → 界是紧的]
  n=100 cap=128 2n-2=198 [成立]
  ```

  n=1、n=2 时一次都没扩容，推导前提不满足。**结论须补一句「至少扩容过一次」**，否则有反例。

### Q8 move_if_noexcept 判据补全 —— **独立答对**

> 没得选。

- 命中 09-21 漏掉的那条：**类型不可拷贝时，即使移动构造不 noexcept 也仍然移动**。
- 完整判据：移动构造 noexcept **或** 类型不可拷贝 → 移动；否则拷贝。

### Q9 copy-and-swap 为什么异常安全 —— **提示后答对**（只给一个反问即通）

先给的两条本人 09-21 已独立答对：

> 1. std::swap 对指针和整数类型的实现就是「用一个临时变量做三次赋值」，
>    全是基本类型赋值，永远不会抛异常。
> 2. noexcept 函数真抛异常，进程立即终止，不析构、不 atexit，即使外层有 try/catch
>    也接不住，因为它在栈展开前就被终止了。

第 3 问本人先答「手写的需要判断自赋值，还需要自己释放旧资源」—— 对，但还在
「代码重复」维度。教练只给一个反问（new 在哪一步、delete 在哪一步、new 抛时 delete 执行了没），
本人随即答对：

> 自己写的拷贝赋值里先 delete 释放了旧资源，然后才申请内存，如果 new 的时候异常，
> 已经执行了 delete，旧的资源也不能使用了；而 copy-and-swap 先进行了拷贝构造/移动构造
> 临时参数对象，如果失败，也没有动 this，这就是强异常保证。

- 定名：**强异常保证 = 可能抛的动作全部发生在碰 `*this` 之前**，不是「出错能回滚」。
- 补名词：noexcept 里抛异常走的是 `std::terminate`。

### Q10 按值传参为何能一函数两用 —— **独立答对（本人自建实验）**

**本人当场驳回教练给的证据**，原话：

> Q10 不对吧，路径 2 和路径 4 是构造啊，看不出来到底是走的移动 operator= 还是拷贝 operator=

**驳回成立**（见第五节教练出错）。本人自建 `test4()` 补实验：

```cpp
void test4() {
    MyVector<D11> a, b;
    b = a;              // 源是左值
    b = std::move(a);   // 源是右值
}
```

教练实跑输出（MSVC，`.vscode/build-msvc.cmd`）：

```
b = a;              → MyVector copy constructor + MyVector copy assignment
b = std::move(a);   → MyVector move constructor + MyVector copy assignment
```

- 结论：形参 `other` 在**调用点**被构造，源是左值就用拷贝构造填它、右值就用移动构造填它，
  函数体内部完全不知情。所以一个函数够了。
- 分辨力要点（本人自己抓到的）：`MyVector<D11> e = a;` 是**初始化走构造**，不是赋值；
  要测 `operator=` 必须有两条**真赋值**路径，且观察点是 MyVector 自己的构造函数打印，
  不是元素 D11 的打印。

## 实现与验证

- 构建：`cd /f/code/cpp-practice/week-03 && cmd /c "$(cygpath -w ../.vscode/build-msvc.cmd) my_vector.cpp verify.exe run"`
- 本人产出：`test4()`（`week-03/my_vector.cpp` 第 62-70 行）
- 教练产出（仅为核验本人断言，非训练代码）：n=1..100 容量探针、左右值两路径对照

## 未掌握 / 卡壳（教练填）

### 🔴 新增：申请内存 ≠ 构造对象 → placement new（**主线必补**，非扩展）

本人自问自答暴露，三小问**全部独立答对**：

> 1. `new T[4]` 这一行，除了要内存，还调用了 T 的构造函数，把 4 个对象都构造出来了
> 2. 因为现在每次内存都是直接 new 出来的，相当于 `data_[size_]` 位置上已经有一个对象了，所以只能赋值
> 3. 标准库不要求（T 有默认构造函数），我的要求

- 这是本人实现与 std::vector 之间**第一个真实能力差距**，不是风格差异：
  本人 MyVector **装不了任何没有默认构造函数的类型**。
- 连带：扩容里的 `temp[i] = std::move_if_noexcept(data_[i])` 走的也是**移动赋值**不是移动构造。
- 欠的部分：如何把「申请内存」与「构造对象」拆开 → **placement new**，文档待本人自查，实现未做。

### 🟡 仍挂账（09-21 自评超纲 2 项）

- 元素级计数 vs 类级打印，为什么后者无分辨力
- `push_back` 用 swap 改造

## 证据与复测

- 当前可证明等级：**C++ L2 达成**（四题三独立答对、一提示后答对，均有自有推导或自有实验）
- 7 天复测日期：2026-09-29
- 21 天迁移测试：2026-10-13

## 五、教练当日自身出错（记录在案，供周复盘核对计划可靠性）

1. **Q10 证据引用错 —— 引用了不在本人当前文件里的实验**（本人当场指出，教练核后收回）：
   09-21 晚与 09-22 早两次拿「证据 9 路径 2/4」当 Q10 的证据。但本人 `my_vector.cpp`
   的 `test3()` 里，`MyVector<D11> e = a;` 与 `MyVector<D11> d = std::move(a);`
   都是**初始化走构造**，整个 test3 里真正的赋值只有最后一条 `c = std::move(b)`，
   **仅一条，无分辨力**。
   根因与老毛病同类：**不核当前来源、照记忆说话**（第 6 次同类）。
   代价：若本人没质疑，一个「用构造路径证明赋值分派」的错误推理会直接写进复测记录。

## 下一天计划

- 下午 17:00 段：Electron 三层链复测 + React/JSX/Vite 闭卷 + `docs/frontend-prereq.md`
- placement new 排进 W3 主线（不许当扩展切掉）
