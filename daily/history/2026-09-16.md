# 2026-09-16（周三）· W2D3

> ⚠️ 本日记录由教练在 23:00 收尾时依据实际证据补写（用户当日未自建记录文件）。
> 「掌握（闭卷自答）」栏只收录用户当天亲口说过的原话，**未代写、未补全**。
> 空缺的自答（Rule of Zero 三个例子、A3 两条结论、A4 第二/三步）如实标为未完成，顺延 09-17。

---

## 完成

### 上午 C++（约 10:15-11:25）

- 闭卷答 Rule of Zero 三问（第 1 问答了，第 2、3 问答不出来）
- 8 分钟闭卷默写「特殊成员函数」名单（不查资料、不看昨天文件）
- 新建 `F:\code\cpp-practice\week-02\day3_rule_of_zero.cpp`（150 行）
  - `Trace`：六个特殊成员函数全部打印，用来分辨实际调了谁
  - `rule1`：`std::string` + `Trace` 成员，一个特殊成员函数都不写
  - `rule2`：显式写 `~rule2() = default`，观察对移动的影响
  - `rule3`：`std::unique_ptr<Trace>` 成员，观察拷贝被删除
  - `MyString`：昨天带裸指针的版本，用于 A3 对照
  - `Test1`~`Test4` 四组实验
- 同时修改了 `day2_gen_rules.cpp`（11:00）

### 下午 Electron / Chromium（约 13:50-14:30）

- 补 A4 **第一步**：在 `F:\code\ai-desktop-assistant` 真的把 `preload` 那行注释掉并跑起来
- 第一次判断为「没报错」→ 实为看错位置（只看了主进程终端，报错在渲染进程 DevTools Console）
- 开 DevTools Console 后抄回真实报错，并做了「注释 preload / 解开 preload」两轮对照，共六行原文
- `main.js` / `preload.js` 今日改动最终已还原（`git diff HEAD` 为空），Electron 侧**无新代码产出**

### 未完成（顺延 09-17）

- ❌ Rule of Zero 的「3 个例子」书面结论
- ❌ A3 的两条结论：「为什么标准不规定 move 之后的具体值」+「一条编码建议」
- ❌ A4 第二步：读 `sandbox.md`（Sandbox 的目标和基本模型）
- ❌ A4 第三步：三问（contextIsolation 隔离谁和谁 / 沙箱与 preload 是否一体两面 / `nodeIntegration: true` 的具体攻击后果）
- ❌ 「今日一句话复述」栏（09-15 起的固定动作，今天漏了）
- ❌ 当日 commit（C++、side project、计划仓库三处全部没提交）

---

## 掌握（闭卷自答，用户原话，未修改）

**Q1：什么样的类可以一个特殊成员函数都不写，还完全安全？**
> 不需要管理资源申请和释放的类。

**Q2 / Q3（Rule of Zero 能否套用 MyString、0/3/5 的关系）**
> 我现在不知道 Rule of 0 / 3 / 5 分别是什么，所以第二问和第三问我都无法回答。

**追问：成员是 `int* p`、构造里 new、析构里 delete，算不算「不需要管理资源」？**
> 不算，因为 int* p 是裸资源。

**8 分钟闭卷默写：特殊成员函数名单**
> 特殊成员函数：析构函数 拷贝构造 拷贝赋值 移动构造 移动赋值

**C++98 / C++11 的分组，以及 0 的含义**
> C++98 应该是析构函数 拷贝构造和移动构造吧，C++11 加了拷贝赋值和移动赋值。0 就是全部不手动实现。

---

## 未掌握 / 卡壳（教练填）

1. **Rule of 0 / 3 / 5 这三个名字，进入本日之前完全没有索引。**
   这不是新知识缺口——09-15 的记录里他自己写过「拷贝类和析构函数会抑制移动构造/移动赋值」，
   机制是有的，缺的是名字和挂载点。这是 STATE.md 里挂了两天的 W1 欠账 C。

2. **C++98 / C++11 的分组答错。**
   他写「C++98 是析构、拷贝构造、**移动构造**；C++11 加了**拷贝赋值**和移动赋值」。
   实际的分界是：C++98 只有析构、拷贝构造、**拷贝赋值**（这三个就是 Rule of 3）；
   C++11 新增的是**移动构造 + 移动赋值**（补成 Rule of 5）。
   他把「拷贝赋值」和「移动构造」两个位置对调了。**这一条 09-17 必须自己重答一次，不许抄本文件。**

3. **第一问的措辞不准。**
   「不需要管理资源申请和释放的类」——任何类的成员都要构造和销毁，字面上说不通。
   准确的说法应落在「自己不直接持有裸资源，资源管理全部委托给已经会管的成员」。
   他在追问里答对了判据（`int* p` 是裸资源），说明判断力有，表述没收紧。

4. **同一个毛病今天第三次犯：「我看的这个指标，真的能反映我想测的东西吗」没问过。**
   - 09-15：traits 输出 1，以为没变化
   - 09-15：typeid 一样，以为类型没变
   - 09-16：`npm start` 终端没报错，以为 preload 摘掉后程序正常
   第三次的表现形式是跨语言的（从 C++ 跑到 JS），说明这是方法问题不是知识问题。

5. **A4 只走完三分之一。** 第一步的实验数据质量很高（见证据），但没读 sandbox.md，
   也没回答三问，「Chromium 沙箱 → contextIsolation → preload 是唯一通道」这条链**没讲通**。
   这是本周门禁项，09-17 下午必须清。

6. **A3 有数据无结论。** Test4 跑出了 MyString 和 `std::string` 的对照输出，
   但「为什么标准不规定 move 之后的具体值」「给自己一条编码建议」两条都没写。
   数据不等于结论，这一步不补就等于没做。

7. **收尾环节整体缺失。** 三个仓库零 commit、记录文件没建、STATE/current 没更新。
   今天的产出是真的，但如果不是 21:31 打卡抓出来，明天他自己都复述不清今天干了什么。

---

## 证据（教练实跑，非推测）

### 1. 编译 `day3_rule_of_zero.cpp`

```
cd /f/code/cpp-practice/week-02
BAT=$(cygpath -w ../.vscode/build-msvc.cmd)
cmd /c "$BAT day3_rule_of_zero.cpp day3_verify.exe"
```

结果：**0 error**，4 条 `C4996`（`strcpy` 安全警告，位于第 38、48、57、92 行），退出码 0。

### 2. 运行输出（`chcp 65001` 下实跑，退出码 0）

```
Test1 ------------------------------------------
Trace 构造函数
--------
Trace 拷贝构造函数
--------
Trace 移动构造函数
Trace 析构函数
Trace 析构函数
Trace 析构函数
Test2 ------------------------------------------
Trace 构造函数
--------
Trace 拷贝构造函数
--------
Trace 拷贝构造函数        ← 关键行：rule2 写了 ~rule2()=default，std::move 静默退化成拷贝
Trace 析构函数
Trace 析构函数
Trace 析构函数
Test3 ------------------------------------------
Trace 构造函数
--------
--------
Trace 析构函数
Test4 ------------------------------------------
yidong gouzao
0                          ← MyString 被 move 走之后 size() == 0（自己写的置零逻辑）
222
3
                           ← std::string 被 move 走之后 c_str() 打印空行
0                          ← std::string 被 move 走之后 size() == 0（MSVC 实测值）
destroy data 123
```

**三处要点（对应验收）**：

- **Test1 vs Test2 是本日最值钱的一对对照**：两个类成员几乎一样，唯一差别是 `rule2` 多写了一行
  `~rule2() = default`。Test1 的第三段输出「移动构造函数」，Test2 的第三段输出「拷贝构造函数」。
  **写一个 `= default` 的析构，就足以让 `std::move` 静默退化成拷贝，编译器一句警告都没有。**
- **Test3 的拷贝报错**：`// rule3 b = a;` 这行被他注释掉并在代码里抄下了原文
  `error C2280: "rule3::rule3(const rule3 &)": 尝试引用已删除的函数`。
  注：抄下报错 ≠ 理解机制。**是哪个成员导致拷贝构造被隐式删除，09-17 要能自己说出来。**
- **Test4 的 `std::string` 一侧数字是 MSVC 实测值**，标准并未规定 move 之后是什么，
  换编译器/换标准库可能不同 —— 这正是「有效但未指定」要说的事，结论他还没写。

### 3. Electron DevTools Console 原文（用户自己抄回）

注释掉 preload 后，点按钮触发：

```
renderer.js:5  Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'openFile')
renderer.js:13 Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'getCurrentData')
renderer.js:21 Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'helloName')
```

Console 里逐条求值：

```
window.electronAPI  →  undefined
process             →  Uncaught ReferenceError: process is not defined
require             →  Uncaught ReferenceError: require is not defined
```

**这组数据分出了三种不同性质的失败**：`window.electronAPI` 是「属性缺失」（求值成功、值为
undefined），`process` / `require` 是「标识符查找失败」（作用域链里压根没这个名字）。
说明它们是被**两套不同机制**挡掉的——前者是 preload 被摘掉，后者是 `nodeIntegration: false`
的默认值，跟 preload 在不在无关。

这条数据直接推翻了原题「删掉 preload.js 后网页里访问 process 会失败」的提法：
渲染进程里 `process` 本来就一直取不到，preload 在不在都一样。

⚠️ 上面这段是**从他实跑的六行数据推出的**，不是他的自答。**A4 的自答仍然欠着。**

### 4. 仓库状态（23:00 实测）

- `F:\code\cpp-practice`：今日新增 `day3_rule_of_zero.cpp`、修改 `day2_gen_rules.cpp`，
  当日 `git log` 为空 → 收尾时由教练补 commit
- `F:\code\ai-desktop-assistant`：`main.js` / `preload.js` mtime 为今日 14:24，
  但 `git diff HEAD` 为空（实验后已还原），无新增产出，**不产生 commit**
- 计划仓库：本记录文件为今日唯一新增

---

## 今日一句话复述

（空缺——09-15 起的固定动作，今天没做。不代写。）

---

## 明日计划（2026-09-17 周四 · W2D4）

见 `daily/current.md`。三件事排在最前：

1. 上午开场 15 分钟收今天的尾：Rule of 0/3/5 用自己的话重答 + C++98/C++11 分组纠错 + A3 两条结论
2. 上午剩余时间做 A1 + B1（异常路径 RAII + `assert` 单元测试）——本周门禁项
3. 下午优先补 A4 第二、三步（sandbox.md + 三问），Menu API 顺延周六
