# 2026-09-20（周日）· W2D7 · 周复盘 + 欠账清算日

> 本文件为**当日流水记录**，周复盘正文在 `daily/history/2026-09-20-weekly.md`（125 行，四块全填）。
> 本文件由教练 23:00 依实跑 / 实查证据补写；闭卷自答与实验代码均为本人产出，教练未代写任何答案。

## 完成（全部有 commit 或实跑证据）

### C++ 侧（欠账清算，五条 + 半条）
- `week-02/day7_debt_clear.cpp`（commit `89a15e9` / `1d579e0`）一个文件清四条 + 额外清一条：
  - `=default` 算用户声明 → D1 走拷贝 / D2 走移动
  - 拷贝构造 `delete` **不连坐**拷贝赋值 → D10 带打印成员逼出调用链
  - move 退化两个缺失例子 → D5 `const`、D6 无移动构造
  - `noexcept` → vector 扩容 → D7 拷贝 / D8 移动
  - 移动构造**反向抑制**拷贝 → test3 `#ifdef` 编译期对照，关 = exit 0 / 开 = exit 2，两条 C2280 + 编译器 note 原话
- `week-02/day7_value_category.cpp`（commit `ccf4aac`）左值/右值值类别：
  五条重载探针 `probe(int&)` / `probe(int&&)`，`test2(int&& x)` 里 `probe(x)` 打印「左值」，**动手前预测命中**
- `week-02/day7_value_category.cpp` test3 补测「左值可取地址」（commit `bf9f125`，17:16）：
  正面 `&a` 打出 `000000E217CFFB50`；反面 `#ifdef` 三条一次放一行——
  `&10` → **C2101**（字面量是纯右值，没有对象也没有存储位置）、`&(a+1)` → **C2102**、
  `&std::move(a)` → **C2102**。字面量与表达式报的是**不同错误码**，编译器本身也把这两者当不同原因。
  → 至此 `docs/cpp-concept-map.md` **零 ❓**

### Chromium/Electron 侧
- 进程模型图 `docs/chromium-process-model.drawio` cell48 / cell49 按订正口径重填（commit `60a50b0`）
- 博客大纲 `blog-drafts/01-why-electron-ai-assistant.md` 四处全改完（commit `60a50b0`）
- **A4 三层链结账**（commit `45ff2f4`，17:28）：本人自核官方 `tutorial/sandbox` +
  `tutorial/context-isolation` + `tutorial/security` 三篇后重答通过，写进 `docs/debt-map.md`：
  1. 沙箱掉 → 拿到渲染进程那个 OS 进程的全部权限
  2. 沙箱在、contextIsolation 掉 → 拿不到 OS，但拿到 **preload 那层权限**；共享同一个 `window`，
     可改 `Array.prototype.push` 等内置方法，**等高权限代码来调用他**
  3. 两层都在、contextBridge 写错仍漏（官方反例：把 `ipcRenderer.send` 整个暴露出去）
  - Q11/Q12 矛盾的准确解法：**开关独立、防护串联**——两句话都对，不矛盾

### 记录与文档
- W2 周复盘 `2026-09-20-weekly.md` 四块全填（投入统计 / 九题闭卷自评 / 门禁逐条验收 / 下周调整）
- 投入统计**本人复核**：W2 = 735min = 12.25h，累计 17.75h，`STATE.md` 原「18.6h」作废
- `docs/cpp-concept-map.md`、`docs/debt-map.md`、`daily/STATE.md`、`daily/progress.jsonl` 四处同步

## 未掌握 / 卡壳（教练填，依实查）

- **Utility vs Renderer 区别**：09-19 空、今天块 2 题 7 仍空，**连续两次落地**。
  ⚠️ 责任在教练：09-19 指的 `mojo_and_services.md` 根本不含该内容。出处待教练实读
  `process_model_and_site_isolation.md` / `sandbox.md` 确认后再布置，**不许再凭记忆指文档**
- **W1 周复盘三处空白**仍在（跨周未补）
- 块 2 九题中 **4 条本人自标「只是看过」**，未转成闭卷可答
- `MyVector<T>` 连续两周未动 —— 已从「有余力」升为 **W3 门禁项**

## 证据（教练实跑 / 实查，非转述）

| 项 | 证据 |
|---|---|
| day7_debt_clear test3 | `#ifdef` 关 = 退出码 0；开 = 退出码 2 + 两条 C2280 + 编译器 note |
| day7_value_category test1/test2 | `probe(x)` 在 `int&& x` 形参内打印「左值」 |
| day7_value_category test3 | `&a` = `000000E217CFFB50`；`&10` = C2101；`&(a+1)` / `&std::move(a)` = C2102 |
| commit 链 | `89a15e9` → `1d579e0` → `ccf4aac` → `bf9f125`（cpp-practice，远端已同步） |
| 计划仓 commit | `60a50b0` → `3386ff2` → `45ff2f4` |
| 知识图状态 | `docs/cpp-concept-map.md` 全文 grep `❓` = 0 条 |

## 方法论进步（三条，本人自发，非教练提示）

1. **「我认为 X 由 ___ 决定」首次执行**，且实验推翻预测后**回头改了结论行**——
   不是把实验当验证仪式，是真拿它改认知
2. **自行发现 `static_assert` 对「`=default` 是否抑制移动」无分辨力**（两侧都为真），
   改用带打印的实例化才分开 —— 这正是连犯三次的那个坑，今天自己绕过去了
3. **当面指出教练「3 分钟的实验说挂到 W3」＝ Vector 拖两周的同一个动作**，已立规矩：
   能当场做完的不许排进下周

## 教练当日出错 2 次（记账）

- 出题诱导：「`BrowserWindow` 对应谁」把答案往进程对象上引
- 把「析构 protected」误标为未补 —— 09-17 history 第 55-57 行本人早已答对，
  **第 4 次「不核来源照记忆排」**

## 明日计划（09-21 周一 · W3D1）

见 `daily/current.md`。三段：C++ 30min（`std::vector` 均摊 O(1) + 迭代器失效）、
Electron 45min（React 引入）、🟣 MyVector 第 1 刀 25min（骨架 + 固定 cap `push_back`）。
