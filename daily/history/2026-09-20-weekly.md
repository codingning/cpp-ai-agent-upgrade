# W2 周复盘（2026-09-14 ~ 2026-09-20）

> 模板由教练生成：题目与空位留白，「证据」栏由教练依实查填满，答案不代写。

---

## 块 1 · 本周投入统计（教练已核，你复核即可）

从 `daily/progress.jsonl` 实读：

| 日期 | minutes | 备注 |
|---|---|---|
| 09-14 | 105 | W2D1 MyString |
| 09-15 | 180 | W2D2 生成规则矩阵 + W1 周复盘补写 |
| 09-16 | 110 | W2D3 Rule of Zero + A4 第一步 |
| 09-17 | 200 | W2D4 欠账全清 + NonCopyable + sandbox.md |
| 09-18 | null | 跨度 10:26-17:45 但混入工具链改造，不估算 |
| 09-19 | 140 | W2D6 进程图 + B2 + B4 |

**W2 合计 = 735 分钟 = 12.25 小时**（09-18 未计入，实际投入高于此数）
**累计 = W1 5.5h + W2 12.25h = 17.75 小时**
`STATE.md` 原写「18.6 小时」已作废，差额 3.2h 来源不明。

- [ ] 我自己加了一遍，数对得上 / 对不上（写哪不对）：对得上

---

## 块 2 · 口头复述自评（本块最重要，教练不代写）

规则：闭上眼，不看任何文件、不看代码，能讲出来才算「自己想明白的」。
讲不出来但看到就认识 = 「只是看过」。能讲但话是我给你的 = 「抄教练的」。
标「抄的」不丢人，标错了才是自欺。

| # | 知识点 | 自己想明白的 / 抄教练的 / 只是看过 | 闭眼讲出来的那句话 |
|---|---|---|---|
| 1 | Rule of 0/3/5 分别是什么，三个数字什么关系 | 自己想明白的 | 0是类不持有裸资源，所以不需要实现任何特殊成员函数。3是实现了析构/拷贝构造/拷贝赋值中任意一个时，通常三个都实现 5是如果你需要手写这五个里的任何一个，通常五个都得写|
| 2 | 6 个特殊成员函数的抑制关系（写了 A 影响 B 吗） | 自己想明白的 | 1.析构/拷贝构造/拷贝赋值 抑制 移动构造/移动赋值（退化成拷贝）。 2.移动构造/移动赋值 抑制 拷贝构造/拷贝赋值delete（编译失败）。 3.写了任意一个构造函数，默认构造就不生成了。（编译失败） |
| 3 | std::move 静默退化成拷贝的三个条件 | 自己想明白的| 1.const T被move时 const T& 转换不成T&&,所以退化 2.移动构造被抑制时（没声明）,退化成拷贝 移动构造=delete时，编译报错3.移动构造未使用noexcept,容器扩容退化成拷贝。|
| 4 | Browser 进程为什么不在沙箱里 | 只是看过 |browser进程需要管理渲染进程/其他进程，并且要访问文件系统、网络、窗口、设备。 |
| 5 | GPU 进程为什么要单开一个 | 只是看过 | 因为gpu需要访问显卡，跑的都是外部驱动代码，安全性未知，为了避免gpu进程出问题时，整个浏览器进程崩溃，所以需要单开一个进程，并使用沙箱。|
| 6 | Network Service 默认跑在哪个进程 | 只是看过| Network Service 是负责网络请求的进程，默认跑在进程外（工具进程内），|
| 7 | Utility 和 Renderer 都是沙箱子进程，区别是什么 |忘记了 | |
| 8 | 沙箱 → contextIsolation → preload 这条链 | 只是看过| 沙箱是最外层防护，默认里面的代码已经被攻破，确保被攻破的代码能碰到哪些东西。上下文是隔离了隔离的是 preload 脚本的 JS 上下文和页面的 JS 上下文。preload是管暴露哪些api给渲染进程的|
| 9 | RenderProcessHost 是什么，Electron 侧最近的对应物是什么 |抄教练的 | RenderProcessHost是browser进程中，表示渲染进程和主进程一条通信通道的对象，代表的是一个进程，对应的是 win.webContents|

> 第 9 题教练 09-20 已订正过口径：RenderProcessHost 代表**一个渲染进程**，
> 不是 ipcMain、也不是 BrowserWindow。Electron 侧最近的可见对象是 webContents。
> 你现在能不能把这句话用自己的话重说一遍，而不是抄上面这行。

---

## 块 3 · W2 门禁逐条验收

对照 `plan/weekly/week-02.md` 文末门禁清单。**逐条判过/不过，不许写「差不多」。**

| 门禁项 | 教练实查 | 你的判定 |
|---|---|---|
| A1 异常路径 RAII + assert 单测 | ✅ `day4_exception_raii.cpp`，commit ff3a0d1 | |
| B1 MyUniquePtr 单元测试 | ✅ `day4_uniqueptr_test.cpp`，同 commit | |
| B2 Chat UI v0 | ✅ `index.html` 4796B，三段式 flex + 气泡，commit 3ec8791 | |
| B3 进程模型图 | ✅ 产出 `chromium-process-model.drawio` 8568B，cell 48 内容错 | |
| B4 blog-drafts + 第 1 篇大纲 | ⚠️ 已建 625B→今日已扩写，原判质量不达标 | |
| A3 move-from 对象状态 | 有实跑数据，结论两条 09-17 补齐 | 已掌握 |
| A4 沙箱→contextIsolation→preload | 三步做完，但 Q11/Q12 矛盾未自核官方文档 | 已掌握 → **教练驳回，见下** |
| C 闭卷默写 6 个特殊成员函数 + 抑制表 | 09-18 签名全对；抑制表首答不合格、重答三格全对 | 已掌握 |
| Vector<T> / IntVector | ❌ 未做（W2 降级为「有余力」） | 已升为 W3 门禁 |
| 5 天日记 + 1 份周复盘 | 09-16、09-19 两天记录由教练补写 | |

### 教练对块 3 的判定

**A3 ✅ 过。** 有实跑数据 + 09-17 补齐两条结论，证据链完整。

**C ✅ 过。** 09-18 签名全对、分组对、抑制表重答三格全对。今天块 2 题 2 再次全对，
是本周唯一一条两次独立验证都过的。唯一瑕疵：Rule of 3 答成表现（今天已纠）。

**A4 ❌ 不过，驳回。** 你判「已掌握」，但这条门禁的原始表述是「一条链讲通」，
而链上有一环你自己 09-17 就标了自相矛盾（Q11 说沙箱和 preload 各管各的，
Q12 说 nodeIntegration:true 会关掉沙箱），09-18 只听了教练结论、没自核官方文档。

教练 09-20 实抓 Electron 官方 `tutorial/sandbox`，原文两句：
- "Sandboxing is tied to Node.js integration. Enabling Node.js integration for a renderer
  process by setting `nodeIntegration: true` disables the sandbox for the process."
- "the environment presented to the preload script is substantially more privileged than
  that of a sandboxed renderer, it is still possible to leak privileged APIs to untrusted
  code running in the renderer process unless contextIsolation is enabled."

所以 Q11 的「各管各的」是错的：三者不是并列三道锁，是**递进的三层**，
且 nodeIntegration 这个开关会直接掀掉第一层。

判定：**A4 挂到 W3 欠账，条件是自己读完官方 sandbox + context-isolation 两篇后重答，
不接受口头「已掌握」。**

---

## 块 4 · 下周调整

### 1. Vector<T> 连续两周没动，W3 怎么排才不会再被挤？

教练已改 `week-03.md`（增量加，未删原有安排）：
周一/二/四各加 25 分钟「追加档」，明确不占用当天原有 C++ 30min + Electron 45min；
三刀 = 骨架/扩容/Rule of 5；MyVector 从「有余力」升为**本周门禁**。

- 你的意见（同意 / 要改）： 同意

### 2. 本周最大盲区是知识点还是方法论？

教练判断：是方法论。「运行前先写预测」6 次未执行，「实验缺分辨力」上周三犯。
**你 09-20 的反驳**：不是不会设计实验，是知识不足以判断「这个实验能不能验证我的要求」。
教练接受此判断，已改前置动作为：动手前写一句「我认为 X 由 ___ 决定」，
写不出来 = 该查文档，不是该跑实验。

- 你的补充：在记忆/知识库中未出现/标明我已经明确掌握的数据，需要你一律按我没有掌握来处理。也就是，在你出题/训练内容时，不能假设我已经掌握了里面所有内容（验证方法/关键字的作用等等），你需要对里面的知识点进行标注和文档补充，或者在出题完成后，先要求我看一遍，是否有哪些地方超出了我的知识范围，根据我的回答，再继续补充文档/内容。

---

## 教练自身错误记录（本周累计，供你核对计划可靠性）

1. 09-17 week-02.md 要求删的 `win.on('closed')` 代码根本不存在
2. 09-19 `mojo_and_services.md` 读法照旧版写，该文档已被上游改写
3. 09-19 该文档不含 GPU/Utility 内容，出题超范围（本人指出）
4. 09-19 断言「Renderer 不直接跟 GPU 说话」——官方文档证伪
5. 09-19 要求删 `overflow-y:auto` 做对照，但从未训练过 CSS 布局（本人指出）
6. 09-19 漏排 week-02 周六的 B2/B4 两个门禁项（本人自查发现）
7. 09-20 「BrowserWindow 对应谁」出题诱导错误，RenderProcessHost 对应的是进程不是窗口
8. 09-20 把 Vector<T> 放进「有余力」= 实质挤掉原安排，违反本人明确规则（第三次同类）
